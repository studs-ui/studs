import ExecutionEnvironment from '@docusaurus/ExecutionEnvironment';
import 'node_modules/@studs/wc';

if (ExecutionEnvironment.canUseDOM) {
  // As soon as the site loads in the browser, register a global event listener
}
