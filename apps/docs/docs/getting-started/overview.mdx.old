---
sidebar_position: 0
---

# Overview

Let's discover **STUDS in less than 5 minutes**.

## Start your site

Run the development server:

```bash
pnpm run build
pnpm run dev
```

The `pnpm run build` and `pnpm run dev` command builds and run your website locally and serves it through a development server, ready for you to view at http://localhost:3000/.
