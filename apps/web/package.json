{
  "name": "web",
  "type": "module",
  
  "main": "src/index.ts",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "tsc",
    "preview": "vite preview",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "@studs/ui": "workspace:*",
    "@studs/styles": "workspace:*"
  },
  "devDependencies": {
    "lit": "^2.7.6",
    "sass": "^1.64.1",
    "typescript": "^5.1.6",
    "vite": "^4.4.7"
  }
}
